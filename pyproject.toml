[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "piosal-generator-portfolio-demo"
version = "0.1.0"
description = "Synthetic C/C++ template code-generation portfolio demo"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
piosal-demo = "piosal_generator.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
